export function Description() {
  return (
    <p>
      As a convention for prototype development, we will place our prototypes in src/pages/dev/
      {'<prototype-name>'}/index.tsx. Components specific to a route live in the -components
      directory alongside that route's index.tsx.
    </p>
  );
}
