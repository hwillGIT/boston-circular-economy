{
  "name": "boston-circular-economy",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev -w client & npm run dev -w server",
    "lint": "npm run lint -w client && npm run lint -w server",
    "replit:build": "cd client && npm install && npx vite build",
    "replit:start": "cd server && npm install && NODE_ENV=production node --import @swc-node/register/esm-register src/index.ts",
    "prepare": "node -e \"try{require('husky').install()}catch(e){if(e.code!='MODULE_NOT_FOUND')throw e}\""
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typedoc": "^0.28.20",
    "typedoc-plugin-markdown": "^4.12.0"
  },
  "overrides": {
    "seroval": "^1.6.2"
  }
}
